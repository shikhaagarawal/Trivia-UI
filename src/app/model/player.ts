export class Player {
  playerName: string;
  sessionId: string;
  gameInstance: string;
  startGame: boolean;
  gameId: number;
  selectedAnswer: number;
  playing: boolean;
  selectedCorrectAnswer:boolean;
}
